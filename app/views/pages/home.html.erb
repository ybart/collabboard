<div class="min-h-screen bg-gradient-to-br from-indigo-50 via-purple-50 to-blue-50">
  <!-- Background Elements -->
  <%= render "pages/home/background_elements" %>

  <!-- Hero Section -->
  <%= render "pages/home/hero" %>

  <!-- Feature Showcase -->
  <%= render "pages/home/features" %>

  <!-- CTA Section -->
  <%= render "pages/home/cta" %>
</div>
